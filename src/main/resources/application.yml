spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/booksclub_mail_service_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.mail.ru
    port: 587
    username: ggntubooksclub@bk.ru
    password: bV6su0SvZOAIJyyvN2nr
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  flyway:
    baseline-on-migrate: true
    enabled: true
    locations: classpath:db/migration
logging:
  level:
    org:
      flywaydb: DEBUG
      springframework:
        jdbc: DEBUG